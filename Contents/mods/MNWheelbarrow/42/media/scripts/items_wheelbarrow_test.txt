module MNWheelbarrow
{
    imports { Base }

    /*
     * VARIANTES DE ORIENTACAO -- descartaveis.
     *
     * A rodada anterior respondeu duas coisas:
     *   - a MASCARA DE ANIMACAO e o que faz o StaticModel renderizar. Sem ela
     *     o modelo nao aparece (testes A e C nao mostraram nada).
     *   - RequiresEquippedBothHands nao impede equipar (A equipou).
     *
     * Sobrou o modelo nascer apontando para baixo, enfiado no chao: o osso da
     * mao tem orientacao propria, diferente da do mundo. Estas variantes usam a
     * configuracao que comprovadamente renderiza -- a do teste B -- e mudam so
     * o giro do mesh.
     *
     * Equipe as tres e diga qual fica de pe.
     */
    item TestRot_X90
    {
        DisplayCategory = Container,
        ItemType = base:normal,
        Weight = 3.0,
        Icon = Toolbox,
        WorldStaticModel = MNWheelbarrow_Ground,
        StaticModel = MNWheelbarrow_Hand_X90,
        primaryAnimMask = holdingbagright,
    }
    item TestRot_Xm90
    {
        DisplayCategory = Container,
        ItemType = base:normal,
        Weight = 3.0,
        Icon = Toolbox,
        WorldStaticModel = MNWheelbarrow_Ground,
        StaticModel = MNWheelbarrow_Hand_Xm90,
        primaryAnimMask = holdingbagright,
    }
    item TestRot_X180
    {
        DisplayCategory = Container,
        ItemType = base:normal,
        Weight = 3.0,
        Icon = Toolbox,
        WorldStaticModel = MNWheelbarrow_Ground,
        StaticModel = MNWheelbarrow_Hand_X180,
        primaryAnimMask = holdingbagright,
    }
}
