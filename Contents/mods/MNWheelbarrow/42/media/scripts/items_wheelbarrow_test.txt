module MNWheelbarrow
{
    imports { Base }

    /*
     * CALIBRACAO DE POSICAO, RODADA 2 -- descartaveis.
     *
     * A rodada 1 respondeu duas coisas:
     *
     *   - TestCont EQUIPOU, entao container + StaticModel + mascara funciona. O
     *     item real nao equipava por outro campo, e o unico ligado as maos que
     *     ele tinha a mais era secondaryAnimMask -- removido de la.
     *
     *   - os eixos do osso da mao NAO sao os do mundo: mexer em Y nao mudou nada
     *     (A e B identicos) e somar Z afundou o carrinho no chao. Ou seja, +Z e
     *     para baixo aqui.
     *
     * Estas tres usam metade do Z que afundou e variam X, para achar o eixo de
     * frente/tras: o carrinho esta aparecendo ATRAS do personagem.
     *
     * Agora sao base:container, iguais ao item real -- nao ha mais motivo para
     * testar com base:normal.
     */
    /* so altura: Z 0.6, metade do que afundou */
    item TestPos_P1
    {
        DisplayCategory = Container,
        ItemType = base:container,
        Weight = 3.0,
        Icon = Toolbox,
        Capacity = 20,
        WorldStaticModel = MNWheelbarrow_Ground,
        StaticModel = MNWheelbarrow_Hand_P1,
        primaryAnimMask = holdingbagright,
    }
    /* altura + X positivo 0.8 */
    item TestPos_P2
    {
        DisplayCategory = Container,
        ItemType = base:container,
        Weight = 3.0,
        Icon = Toolbox,
        Capacity = 20,
        WorldStaticModel = MNWheelbarrow_Ground,
        StaticModel = MNWheelbarrow_Hand_P2,
        primaryAnimMask = holdingbagright,
    }
    /* altura + X negativo 0.8 */
    item TestPos_P3
    {
        DisplayCategory = Container,
        ItemType = base:container,
        Weight = 3.0,
        Icon = Toolbox,
        Capacity = 20,
        WorldStaticModel = MNWheelbarrow_Ground,
        StaticModel = MNWheelbarrow_Hand_P3,
        primaryAnimMask = holdingbagright,
    }
}
