module MNWheelbarrow
{
    imports { Base }

    /*
     * VARREDURA DE BASE DA ROTACAO DA MAO -- descartaveis.
     *
     * Oito itens identicos entre si em tudo, menos no modelo de mao: A e a
     * identidade, B ate G sao as seis rotacoes de 90 graus nos eixos, H e 180
     * em X. Ver models_wheelbarrow_test.txt para o porque de a varredura ser
     * feita de uma vez em vez de por bisseccao.
     *
     * Sao base:container e usam a mesma mascara do item real, para que o unico
     * campo diferente entre eles e o carrinho de verdade seja o modelo.
     *
     * Nomes sao TestRot_<letra> justamente para ordenarem juntos na busca do
     * menu de debug -- digitar "TestRot" traz os oito.
     */
    item TestRot_A { DisplayCategory = Container, ItemType = base:container, Weight = 3.0, Icon = Toolbox,
        Capacity = 20, WorldStaticModel = MNWheelbarrow_Ground, StaticModel = MNWB_RotA, primaryAnimMask = holdingbagright, }

    item TestRot_B { DisplayCategory = Container, ItemType = base:container, Weight = 3.0, Icon = Toolbox,
        Capacity = 20, WorldStaticModel = MNWheelbarrow_Ground, StaticModel = MNWB_RotB, primaryAnimMask = holdingbagright, }

    item TestRot_C { DisplayCategory = Container, ItemType = base:container, Weight = 3.0, Icon = Toolbox,
        Capacity = 20, WorldStaticModel = MNWheelbarrow_Ground, StaticModel = MNWB_RotC, primaryAnimMask = holdingbagright, }

    item TestRot_D { DisplayCategory = Container, ItemType = base:container, Weight = 3.0, Icon = Toolbox,
        Capacity = 20, WorldStaticModel = MNWheelbarrow_Ground, StaticModel = MNWB_RotD, primaryAnimMask = holdingbagright, }

    item TestRot_E { DisplayCategory = Container, ItemType = base:container, Weight = 3.0, Icon = Toolbox,
        Capacity = 20, WorldStaticModel = MNWheelbarrow_Ground, StaticModel = MNWB_RotE, primaryAnimMask = holdingbagright, }

    item TestRot_F { DisplayCategory = Container, ItemType = base:container, Weight = 3.0, Icon = Toolbox,
        Capacity = 20, WorldStaticModel = MNWheelbarrow_Ground, StaticModel = MNWB_RotF, primaryAnimMask = holdingbagright, }

    item TestRot_G { DisplayCategory = Container, ItemType = base:container, Weight = 3.0, Icon = Toolbox,
        Capacity = 20, WorldStaticModel = MNWheelbarrow_Ground, StaticModel = MNWB_RotG, primaryAnimMask = holdingbagright, }

    item TestRot_H { DisplayCategory = Container, ItemType = base:container, Weight = 3.0, Icon = Toolbox,
        Capacity = 20, WorldStaticModel = MNWheelbarrow_Ground, StaticModel = MNWB_RotH, primaryAnimMask = holdingbagright, }
}
