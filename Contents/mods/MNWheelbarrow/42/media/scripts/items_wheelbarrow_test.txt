module MNWheelbarrow
{
    imports { Base }

    /*
     * VARIANTES -- descartaveis. Duas perguntas nesta rodada.
     *
     * 1) POSICAO. Xm90 foi a orientacao correta -- o carrinho fica de pe -- mas
     *    saiu na linha dos ombros. As tres primeiras variam so o deslocamento:
     *
     *      PosA  desce 1.2
     *      PosB  desce 1.9
     *      PosC  desce 1.9 e avanca 1.2, para testar se +Z e a frente
     *
     *    As outras rotacoes ficaram descartadas: X90 deixou o carrinho de cabeca
     *    para baixo e X180 nas costas, como mochila.
     *
     * 2) POR QUE O CONTAINER NAO EQUIPA. As variantes anteriores eram
     *    base:normal e equipavam; o item real e base:container e nao equipa, nem
     *    depois de remover RequiresEquippedBothHands. TestCont e identico as
     *    outras, so que container -- isola exatamente essa diferenca.
     */
    item TestPos_A
    {
        DisplayCategory = Container,
        ItemType = base:normal,
        Weight = 3.0,
        Icon = Toolbox,
        WorldStaticModel = MNWheelbarrow_Ground,
        StaticModel = MNWheelbarrow_Hand_PosA,
        primaryAnimMask = holdingbagright,
    }
    item TestPos_B
    {
        DisplayCategory = Container,
        ItemType = base:normal,
        Weight = 3.0,
        Icon = Toolbox,
        WorldStaticModel = MNWheelbarrow_Ground,
        StaticModel = MNWheelbarrow_Hand_PosB,
        primaryAnimMask = holdingbagright,
    }
    item TestPos_C
    {
        DisplayCategory = Container,
        ItemType = base:normal,
        Weight = 3.0,
        Icon = Toolbox,
        WorldStaticModel = MNWheelbarrow_Ground,
        StaticModel = MNWheelbarrow_Hand_PosC,
        primaryAnimMask = holdingbagright,
    }

    /* So muda o ItemType em relacao as de cima. */
    item TestCont
    {
        DisplayCategory = Container,
        ItemType = base:container,
        Weight = 3.0,
        Icon = Toolbox,
        Capacity = 20,
        WorldStaticModel = MNWheelbarrow_Ground,
        StaticModel = MNWheelbarrow_Hand_PosB,
        primaryAnimMask = holdingbagright,
    }
}
