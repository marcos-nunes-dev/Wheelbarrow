module MNWheelbarrow
{
    imports { Base }

    /*
     * CALIBRACAO DE POSICAO, RODADA 4 -- descartaveis.
     *
     * A rotacao esta resolvida e nao e mais variavel: 180 em X, previsto pela
     * relacao aparencia-em-jogo = mundo(180 + T) e validado contra quatro
     * observacoes de tela. As tres variantes abaixo compartilham essa pose.
     *
     * Sobrou uma incognita: qual eixo e frente/tras. O lateral e o X (medido na
     * rodada 2: +0.8 mandou o carrinho para a mao esquerda, -0.8 para longe a
     * direita) e fica em ZERO nas tres, senao o resultado fica ambiguo.
     *
     * ATENCAO ao comparar com o item real: ele agora usa a mesma pose da P1.
     * Se ele se comportar diferente das variantes, a causa esta nos campos que
     * so ele tem -- e nao no modelo.
     */
    /* so a altura */
    item TestPos_P1
    {
        DisplayCategory = Container,
        ItemType = base:container,
        Weight = 3.0,
        Icon = Toolbox,
        Capacity = 20,
        WorldStaticModel = MNWheelbarrow_Ground,
        StaticModel = MNWheelbarrow_Hand_P1,
        primaryAnimMask = holdingbagright,
    }
    /* altura + Z negativo 0.7 */
    item TestPos_P2
    {
        DisplayCategory = Container,
        ItemType = base:container,
        Weight = 3.0,
        Icon = Toolbox,
        Capacity = 20,
        WorldStaticModel = MNWheelbarrow_Ground,
        StaticModel = MNWheelbarrow_Hand_P2,
        primaryAnimMask = holdingbagright,
    }
    /* altura + Z positivo 0.7 */
    item TestPos_P3
    {
        DisplayCategory = Container,
        ItemType = base:container,
        Weight = 3.0,
        Icon = Toolbox,
        Capacity = 20,
        WorldStaticModel = MNWheelbarrow_Ground,
        StaticModel = MNWheelbarrow_Hand_P3,
        primaryAnimMask = holdingbagright,
    }
}
