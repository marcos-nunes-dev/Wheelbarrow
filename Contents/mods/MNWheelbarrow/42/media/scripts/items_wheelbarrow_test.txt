module MNWheelbarrow
{
    imports { Base }

    /*
     * DESCARTAVEL -- uma pergunta so, sai antes de publicar.
     *
     * PERGUNTA: preso a um OSSO, o carrinho continua visivel durante as acoes?
     *
     * Hoje ele e desenhado por StaticModel, que so aparece enquanto a MASCARA de
     * animacao esta ativa. Toda acao cronometrada substitui a animacao do
     * personagem, e o carrinho some -- ao largar, ao transferir itens do chao. O
     * jogo base tem a mesma limitacao: em media/AnimSets/player/maskingright ha
     * variante de mascara para andar, correr, agachar, mirar e cair, e NENHUMA
     * para estado de acao.
     *
     * O outro caminho e ReplaceInPrimaryHand com um clothing item, que prende o
     * modelo a um OSSO. Osso nao depende de mascara, entao a hipotese e que ele
     * sobreviva as acoes. E o que o mod Apocalypse Wheelbarrows usa, e por isso
     * vale testar em vez de deduzir.
     *
     * O clothing item usa m_Static = true, ou seja malha comum, sem rigging --
     * outra coisa que eu tinha descartado por engano achando que exigia skin.
     *
     * O QUE ESPERAR DE ERRADO: a posicao. A malha de mao esta assada para a pose
     * do StaticModel, e o osso de anexacao tem origem propria. Se o carrinho
     * aparecer deslocado, isso NAO invalida o teste -- a pergunta e so se ele
     * continua visivel durante a acao. Posicao se recalibra depois.
     */
    item TestBone
    {
        DisplayCategory = Container,
        ItemType = base:container,
        Weight = 3.0,
        Icon = Wheelbarrow,
        Capacity = 50,
        WorldStaticModel = MNWheelbarrow_Ground,
        ReplaceInPrimaryHand = MNWB_CartHand mnwb_holdingcart,
        RequiresEquippedBothHands = TRUE,
    }
}
